name: Deploy Cloudflare Worker

# Automatic deployment is intentionally paused until the current
# production Worker source has been synchronized into this repository.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy paused pending Cloudflare source sync"
